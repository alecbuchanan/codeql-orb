description: >
  Initializes the CodeQL CLI
# What will this job do?

executor: default

parameters:
  cache:
    type: boolean
    default: false
    description: "Store CodeQL Bundle in CircleCI cache for faster builds"

steps:
  - init:
      cache: << parameters.cache >>
